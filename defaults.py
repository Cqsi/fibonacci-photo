# This is the default image path. Edit this to be the path of the image you want
path_of_image = "test_images\\fibonacci.png"

# This is a limit on how many squares to generate
# You can try bigger values but the bigger you get the longer it takes
fibonacci_limit = 9

# Creates a vertical picture when True, otherwise creates a horizontal one when False
vertical_rectangle = True